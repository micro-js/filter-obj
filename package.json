{
  "name": "@f/filter-obj",
  "description": "Functional, data-last object filter.",
  "repository": "git://github.com/micro-js/filter-obj.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
